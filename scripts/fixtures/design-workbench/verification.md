# 设计能力样例验证

2026-09-11 02:02（Asia/Shanghai）运行 `pnpm design:check`，本机 Chromium 通过，浏览器错误为零。

- PC：列表与搜索、空态、加载失败、无权限、长标题；真实字段校验、等待、失败后保留输入、继续编辑与放弃确认、提交完成。
- 移动端：列表、详情、返回、真实移动字段、选择面板与保存；390 × 844 视口无横向溢出。
- 主题：默认 → 自定义 → 默认切换保持已有输入；下拉、确认框、消息和通知处于应用作用域；移动字段继承应用 token，相邻应用和 body 保持原样。
- 视觉检查：人工查看 PC、移动列表/详情/表单与错误恢复截图；修复了 token 容器类名冲突、移动 token 被默认值遮盖、减少动画时弹层停留在缩放零、移动选择面板页脚误受样例样式影响等实际问题。

执行脚本会重建 `.cache/design-workbench-evidence/report.json` 与五张截图；这些运行产物不纳入版本库。截图只是本次组件集成证据。本样例使用本地模拟数据，不代表真实平台 API、真实权限验收或用户对具体视觉的确认；AppSpec 视觉记录仍为 draft。
