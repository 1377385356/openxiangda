# Consumed prerelease Changeset history

These files are the verbatim, already-consumed Changesets listed in
`.changeset/pre.json`. They remain in Git for audit history but live outside the
active `.changeset/` input directory so Changesets cannot treat references to
retired prerelease packages as new release inputs.

Do not edit these records. New reviewed release input belongs in `.changeset/`.
