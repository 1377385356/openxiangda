---
---

Resolve `department_supervisor` workflow bindings from the local department
manager approval scope so fresh full-stack applications match the platform
provider during PostgreSQL lifecycle verification. Keep its direct assignment
semantics through the PostgreSQL transaction guard even when an unrelated
long-term delegation exists for the same role subject.
