---
"openxiangda-contracts": minor
"openxiangda-compiler": minor
"openxiangda-devkit-core": minor
---

Add the bounded Native RoleSession context, paged role-subject and CAS switch
wire contracts. Require the platform capability from every Native AppPackage,
expose explicit devkit methods, and make the local platform implement the same
identity protocol without returning complete scope grants.
