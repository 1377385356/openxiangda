---
---

Move the local PostgreSQL and browser mock Data API exclusively onto the Native 2.0 kernel route. Local development, packaged application tests, and remote runtimes now exercise one route contract without a legacy fallback.
