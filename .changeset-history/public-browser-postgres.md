---
"create-openxiangda": patch
"openxiangda-admin": patch
"openxiangda-nest": patch
---

Allow generated Playwright suites to target an already supervised full local session so browser acceptance can cover the same NestJS and PostgreSQL lifecycle instead of always starting UI-only mode. Run that suite once inside a freshly generated external tarball application whose database is reset only through the supervised CLI, keeping developer data untouched while making release evidence deterministic. Close the local App API identity loop on the Native Principal and RoleSession contracts, including the generated NestJS application template, so browser workflow acceptance exercises the same 2.0 identity model as a deployed application. Keep the Admin header compact by separating the selected role name from its bounded scope description, show workflow and data records with business-facing labels, and migrate dirty-form confirmation to the current Ant Design 6 Modal API.
