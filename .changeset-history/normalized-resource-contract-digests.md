---
"openxiangda-compiler": patch
---

Compute Data Resource schema contract digests from the normalized config bundle representation so the platform can independently verify the immutable artifact bytes.
