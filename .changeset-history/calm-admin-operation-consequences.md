---
"openxiangda-admin": minor
"create-openxiangda": patch
---

Refine the reviewed Admin v2 visual baseline for standard data management and
workflow detail pages. Data tables now keep search, batch operations, density,
column settings, import/export and destructive actions in predictable B2B
locations, while Workflow Surface operations use a consequence-aware drawer
driven by the backend `uiSchema` protocol. The local platform mirrors the same
confirmation contract and generated applications verify it in the browser.
