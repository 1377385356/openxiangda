---
'openxiangda-admin': minor
'create-openxiangda': minor
---

Add a standard application credentials console for platform-managed runtime OAuth2 identities, external OAuth2 clients, write-only application Secrets and Workflow assignee-provider rotations. Extend the official application template, local platform mock, lazy-route build gate and Chromium acceptance so a newly created app can verify the complete secure credential lifecycle without provisioning or publishing first.
