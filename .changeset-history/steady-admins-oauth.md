---
"openxiangda-contracts": minor
"openxiangda-devkit-core": minor
"openxiangda-nest": minor
"openxiangda-admin": minor
"create-openxiangda": minor
"openxiangda-cli": minor
"openxiangda-compiler": minor
---

Add tenant/application/environment-bound OAuth2 client credentials, audited client lifecycle commands and service-principal NestJS authorization. Add requestless NestJS OAuth2 token single-flight, expiry refresh and one-time 401 recovery together with an application-identity Data API facade for Worker and Scheduler code; deployed backends consume platform-injected workload credentials instead of declaring reserved Secrets. Add a two-phase platform-managed workload credential rotation that stages by version CAS, keeps old Pods valid during a bounded grace period, activates only while preparing a same-AppVersion rolling deployment, never returns runtime Secrets to the CLI and makes retries deterministic through one idempotency key. Complete the Ant Design 6 admin shell and server-driven data-management page with batched authorization, field policies, role-scoped tab/list preferences, scope-aware identity switching, proactive RoleSession renewal, non-blocking identity recovery, dashboards, Directory v2 department/user/relation controls, readable labels, permission-aware CSV import/export, stale-query protection and route/data error recovery. Add single-record details, correlated durable business-audit timelines, bounded server-side aggregate charts and lifecycle-managed attachment fields with signed upload, protected download and delayed cleanup. Add a standard application operations page covering platform/backend readiness, immutable versions, deployments, event retries and dead letters. Add narrow Admin package entrypoints, lazy business routes and executable production chunk budgets. Add environment-scoped runtime Secret declarations and operational event commands, make Workflow Provider rotation deployment-safe, validate and deduplicate provider candidates, and add platform-backed durable event receipts with idempotent acknowledgement retry and safe post-handler failure semantics. Verify sealed package and artifact bytes immediately before upload, add a publish-free local SDK workflow with install-before-generate scaffolding, automatically refresh generated contracts, prevent stale packaged template snapshots, and ship a development-only platform mock with one-shot 401/403/409/503/latency fault injection for independent browser acceptance. Compile the fault laboratory out of production bundles and enforce that isolation in the build gate. Add a packed-distribution smoke gate that installs the real tarballs and creates a new application outside the monorepo, then runs generation, type checks, unit tests, full Chromium Admin acceptance and production build under a fail-closed pnpm dependency build policy that permits only reviewed lifecycle scripts.
