---
"openxiangda": minor
"openxiangda-cli": patch
"openxiangda-devkit-core": patch
"openxiangda-mcp": patch
"openxiangda-skill-kit": patch
---

Remove the OpenDesign runtime, native CLI bridge, command, vendored guidance and
upstream maintenance workflow. Interface work now stays in one Agent-owned loop:
use Image 2.5 or another available image capability only when a visual reference
helps, implement the real React page directly, and iterate with browser evidence.
AppSpec design assets, platform Shell and field behavior, scoped theme inputs,
permissions and real-role acceptance remain authoritative and compatible.
