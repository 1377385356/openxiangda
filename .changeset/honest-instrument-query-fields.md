---
"openxiangda-cli": patch
---

Keep the instrument golden module list query within its declared Native Data
fields, display the platform `updated_at` system value without sorting on it,
and cover the full browser CRUD continuation.
