---
"openxiangda": minor
"openxiangda-devkit-core": minor
---

Expose bounded Resource record and App Operation JSON Schema composition helpers
through `openxiangda/config`. The helpers project the canonical public field
value schemas from one Resource declaration, clone shared protocol constants,
and permit only validated local `$defs` and `$ref` composition.
