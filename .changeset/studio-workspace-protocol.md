---
"openxiangda-contracts": minor
"openxiangda-devkit-core": minor
"openxiangda-cli": minor
"openxiangda-mcp": patch
"openxiangda": minor
---

Add the Studio workspace protocol without creating a second AI or authorization
path. The existing pinned CLI now emits ordered JSONL lifecycle events for its
supported long-running commands, creates applications with explicit identity
and verified immutable template bindings, and advertises the protocol through
the existing workspace context and MCP resource.
