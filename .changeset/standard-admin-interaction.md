---
"openxiangda": patch
"openxiangda-devkit-core": minor
"openxiangda-cli": patch
"openxiangda-skill-kit": patch
---

Simplify the standard admin shell and list, add grouped drawer entry using the
same standard form lifecycle as full-page entry, and retain user input on failed
saves and background refreshes.
Expose shared form/detail section declarations on explicit CRUD views without
adding layout metadata to storage models or changing the Data API schema.
