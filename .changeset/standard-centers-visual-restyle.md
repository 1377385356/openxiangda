---
'openxiangda': patch
---

标准待办中心与消息中心视觉重构：

- 修复运行时基础字体缺失导致的裸元素衬线（宋体）渲染；字体/字号/行高通过 `--ant-*` 变量跟随应用主题。
- 待办中心改为 layout 底 + 内容卡层次，页头/表格/移动卡片全面翻新；消息中心控制区单行对齐、摘要卡与未读标识强化、移动卡片未读左缘条。
- 两个页面的测试锁定类名、角色与文案保持不变；`verify-design-workbench` 改用空闲临时端口，修复本地 dev server 占用 5173 时设计门必失败的问题。
