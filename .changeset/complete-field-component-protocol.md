---
"openxiangda-contracts": major
"openxiangda-devkit-core": major
"openxiangda-nest": major
---

Replace the pre-release physical field vocabulary with the complete semantic
field protocol, snapshot value schemas, bounded typed query tree, exact
generated record/write types, widget and source compatibility, deterministic
physical/query plans, and closed resource-aware authorization field paths.
The standard Data API also gains one bounded streamed CSV export request that
reuses the semantic query contract, while authored UUID fields become
platform-generated immutable identifiers.
