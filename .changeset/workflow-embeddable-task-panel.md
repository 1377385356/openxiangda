---
"openxiangda": patch
---

Expose a reusable `WorkflowTaskOperationsPanel` for application-owned Workflow detail pages and make the standard task page use the same operation renderer and refresh behavior.
