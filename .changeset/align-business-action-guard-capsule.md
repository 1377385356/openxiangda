---
"openxiangda-cli": patch
"openxiangda": patch
---

Align the generated application template capsule with the globally enforced
Nest business-action guard so `create` and `check` require one exact SDK line.
