---
"openxiangda": patch
---

Expose bounded workbook reading and explicit column mapping, partial-update validation, blank-cell policy, and source row numbers through the shared field-kit import parser. Existing standard import defaults remain unchanged.
