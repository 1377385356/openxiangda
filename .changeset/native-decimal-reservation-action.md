---
"openxiangda-contracts": minor
"openxiangda-devkit-core": minor
---

Introduce a bounded immutable Named Action declaration for Native exact-decimal reservations and terminal transitions. Both compilers validate the same resource field mapping and status values, preserve the declaration in the sealed configuration, and require the new platform capability. Runtime activation remains gated on the platform transaction implementation.
