---
"openxiangda-contracts": patch
"openxiangda": patch
---

Add canonical Workflow instance sequencing and resource navigation targets, plus standard desktop submission, work-center, task, instance pages and independent mobile work-center/task/instance surfaces. Application templates now wire generated Workflow codes into the unified 2.0 React runtime without adding a second UI package.
