---
"openxiangda-contracts": patch
"openxiangda-devkit-core": patch
"openxiangda-nest": patch
"openxiangda": patch
---

Type Workflow command results across the browser, NestJS, and DevKit clients,
and make standard task completion skip stale task refreshes when the platform
advances the workflow. Expose the command result to React consumers so they
can load the resulting instance while preserving legacy Surface callbacks for
refreshable task and instance commands.
