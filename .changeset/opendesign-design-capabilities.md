---
"openxiangda": minor
"openxiangda-devkit-core": minor
"openxiangda-skill-kit": minor
"openxiangda-cli": minor
---

Integrate pinned OpenDesign methods and craft into the default design workflow and same-version Skill, CLI and MCP documentation. Bind prototype and design-system assets to AppSpec review digests, including frozen-commit acceptance. Let applications supply scoped visual themes while retaining platform field, navigation and authorization behavior.

Use the original OpenDesign desktop and complete native CLI as the default design experience through a thin discovery and command bridge. Preserve upstream projects, resources, updates, agent configuration, arguments, output and exit behavior without replacing the upstream runtime.
