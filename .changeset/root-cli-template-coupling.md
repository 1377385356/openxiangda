---
"openxiangda": patch
"openxiangda-cli": patch
---

Keep the CLI packaged template versioned with the root application SDK and reject root-only release Changesets before version materialization. Includes the unpublished advanced query operand correction.
