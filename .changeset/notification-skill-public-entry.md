---
"openxiangda": minor
---

Export the business-action and signed-event notification schema constants from
the existing `openxiangda/nest` application facade. The packaged backend Skill
now uses that public entry, while the physical contracts capsule remains
private and unavailable as `openxiangda/contracts`.
