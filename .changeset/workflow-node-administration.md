---
"openxiangda-contracts": patch
"openxiangda-devkit-core": patch
"openxiangda-cli": patch
"openxiangda-skill-kit": patch
---

Add typed, environment-scoped reads for application administration and Workflow node runtime configuration. Developers and AI can inspect code defaults, administrator overrides, effective parameters and revision history with `openxiangda admin`; existing tasks retain their frozen configuration while future node entries adopt current settings.
