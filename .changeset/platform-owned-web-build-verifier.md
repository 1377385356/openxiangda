---
"openxiangda": patch
"openxiangda-cli": patch
---

Move the generic Web build and bundle-budget verifier into the versioned platform package so generated and existing 2.0 applications consume one npm-owned policy instead of copied source rules.
