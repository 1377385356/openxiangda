---
"openxiangda": patch
---

Replace direct file-picker imports with the standard field-aware import workspace: generate permission-scoped XLSX templates and field guides, validate semantic field values before preview, and keep parse failures inside the import dialog instead of blanking the application page.
