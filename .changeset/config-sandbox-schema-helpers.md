---
"openxiangda": minor
"openxiangda-cli": minor
"openxiangda-devkit-core": minor
---

Make the CLI configuration sandbox expose the same canonical App Operation
schema-composition helpers as the public `openxiangda/config` entry. Resource
record schemas, local references and composed request/response definitions can
now be executed by `openxiangda check` without copying managed-file protocols.
