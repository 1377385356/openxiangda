# 自定义用户框架退出登录出口

应用自定义frame没有公开logout方法，现有Shell则调用平台客户端logoutCurrentUser。公开同一个函数及返回类型即可，禁止应用复制身份缓存、广播或深层导入内部文件。

本次只增加openxiangda/react导出，不修改退出协议：成功后现有实现清理当前身份/授权加载/CSRF并发布跨页面退出事件，返回平台redirect receipt；失败不会假装已退出。导航由调用者按receipt处理。只影响V2公开入口，回滚移除导出；发布构建和已有退出浏览器测试验证协议保持不变。
