# 中文单字目录检索

PL-20 浏览器实测：后端已支持李，PC 选择器仍以至少两个字符阻止发请求。
平台 Directory API 继续拥有权限、租户、分页及检索结果；组件只校验输入。
PC/移动、成员/部门统一允许单个 Unicode 汉字，保留 2–64 字符普通查询。
不改变 debounce、游标、选择、持久化或授权。不影响独立 V1。
无效输入显示同一提示，不发请求；空输入保留组织浏览。
验证纯输入边界及发布浏览器选择器矩阵，应用升级后实测李的分页搜索。
回退为原 SDK 包版本，不涉及数据回滚；要求平台 server 含 PL-20 修复。
