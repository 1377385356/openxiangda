# 已接受命令的浏览器恢复验收时序

正式 alpha.114 候选分发中，95 个浏览器场景曾通过；下一次相同包字节的检查在等待短暂状态警告时失败，94 通过、1 失败、6 条件跳过。失败指针为 workflow-entry.spec.ts 中 transient command status read，不能直接重试到通过或延长超时。

所有者是维护者浏览器 fixture，公开运行逻辑保持原样：已接受命令在一次 500 后自动读取原命令，不能再次提交业务写入。原 fixture 的第二次状态读取立即成功，警告可能在自动等待采样前消失。验收通过一个可释放的网络响应闸门稳定展示中间状态：首读明确 500，自动第二次只读请求进入等待；确认警告、原提交唯一及确有自动读取后，放行成功响应，再确认完成回调和业务副作用各一次。闸门仅位于 Playwright 路由，不加入 SDK、模板或应用运行代码。

失败轨迹的旧临时工作区已被分发脚本清理，原失败日志仍保留；新的定向验证保留截图与 trace。使用同一冻结 tarball 独立安装，连续三次定向验证，随后正常正式门禁。无新增公开契约、Changeset 或平台部署影响；回退只删除此测试闸门。


同一冻结 alpha.114 包的独立安装定向场景连续 3 次通过（8.9 秒），截图确认警告、灰化提交按钮及状态刷新入口可见；随后自动恢复成功且提交请求始终只有一次。正式全量分发门禁继续执行。
