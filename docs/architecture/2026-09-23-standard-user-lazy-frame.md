# 标准用户页面懒加载时保留框架

证据：工作中心首次进入时外层Suspense将整页替换为“正在加载审批页面…”。框架由应用contributions.standardUserSurfaces独占，平台不新增第二框架。将仅页面内容的Suspense放入StandardUserPageSurface，使已挂载的导航、用户入口和返回行为在懒加载时保留；权限、错误边界和动态路由保持原所有者。桌面/移动共用该路径，非用户页面不变。延迟真实工作流模块响应验证框架在pending期间仍存在，释放后内容出现且框架DOM节点未替换；失败仍由既有错误边界处理。可独立回退这一渲染顺序变化，不变更身份或数据。
